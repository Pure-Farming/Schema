{
    "$schema": "https://json-schema.org/draft/2019-09/schema",
    "description": "Defines basis for the greenhouse gas intensity metric. Most models provide one or a subset of these intensity measures.",
    "type": "string",
    "enum": [
        "Area",
        "GrazedArea",
        "EffectiveArea",
        "ProductWeight",
        "Head"
    ]
}